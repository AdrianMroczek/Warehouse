pipeline {
  agent any
  environment {
    IMAGE_NAME = "warehouse-app"
    CONTAINER_NAME = "warehouse-app-container"
  }
  stages {
    stage('Prepare DB') {
      steps {
        sh "./scripts/restock.sh"
      }
    }
  }
}